package unsw.dungeon;

/**
 * The Enum Direction.
 */
public enum Direction {
	
	/** The up direction. */
	UP,
	
	/** The down direction. */
	DOWN,
	
	/** The left direction. */
	LEFT,
	
	/** The right direction. */
	RIGHT,
	
	/** Unable to move. Used for enemy behaviour only. */
	UNABLE
}
